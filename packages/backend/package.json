{
  "name": "@abyssal/backend",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "docker compose build",
    "test": "docker compose run --rm api pytest",
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down -v"
  }
}
