{
  "name": "@abyssal/backend",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "../../.venv/bin/pip install -r requirements.txt",
    "start": "../../.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 3334 --reload",
    "test": "../../.venv/bin/pytest -q"
  }
}
