{
  "name": "@abyssal/backend",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "mvn -f pom.xml package",
    "test": "mvn -f pom.xml test",
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down -v"
  }
}
